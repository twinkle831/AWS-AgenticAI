# Copy to .env and fill in values (optional — many work from AWS CLI)

# AWS — used for Bedrock (agents) + DynamoDB, Step Functions, IoT
# Run: aws configure   (then you often don't need to set these)
AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_PROFILE=default

# Store
STORE_ID=store-001

# Optional: Bedrock model (default: Claude 3 Haiku)
# BEDROCK_MODEL_ID=anthropic.claude-3-haiku-20240307-v1:0
